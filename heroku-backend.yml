# heroku.yml - Configuração para deploy do backend
# Use este arquivo para o app backend

build:
  languages:
    - python
  
setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

run:
  web: cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT
